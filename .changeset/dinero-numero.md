---
"@datoteca/cl": minor
---

Agrega `dinero.clpNumero()` y `dinero.ufNumero()`, variantes numéricas de `dinero.clp()`/`dinero.uf()` para quien necesite operar los valores (sumar, comparar, etc.) en vez de recibir el string ya formateado.
